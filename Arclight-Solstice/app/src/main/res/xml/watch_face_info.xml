<?xml version="1.0" encoding="utf-8"?>
<WatchFaceInfo>
    <Preview value="@drawable/preview" />
    <MultipleInstancesAllowed value="false" />
    <!-- Editable=true is what surfaces the on-watch / companion customization editor
         (themes, toggles, complication slots). Without it the face renders but can't be edited. -->
    <Editable value="true" />
</WatchFaceInfo>
