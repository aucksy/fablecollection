# CONTEXT — kickoff for a fresh chat

You are continuing **Fable Watch Faces** (Series 01 · ARCLIGHT + Series 02 · LEDGER) at `D:\Apps\WearOS Apps\WatchFaces\Fable Collection`.

1. Read `PROGRESS.md` (state + next steps + risk watchlist) and `README.md` (structure).
2. Design sources of truth: `FABLE-Arclight-Showcase.html` (Series 01), `FABLE-Ledger-Showcase.html` (Series 02) and `FABLE-Armature-Showcase.html` (Series 03) — 02/03 are **feasibility-locked**: every signature names its proven WFF mechanism; keep that discipline when building. Deep links like `#f=0&cfg=Full&mode=aod`. Do NOT look at the separate TwelveSixty project — owner wants this line independent.
3. Build rules: cloud-only (no local Android toolchain on this machine — never download it). Ship loop: push main → tag `v*` → GitHub Actions builds and releases the APK → paste the direct `.apk` download link in chat. Adversarial review BEFORE tagging.
4. Repo: ONE monorepo `github.com/aucksy/fablecollection` — but **ONE APK PER FACE** (decided 2026-07-10; WFF complications are Scene-global so per-face complications need a package per face). 5 series × 5 faces = 25 top-level Gradle projects, named `<Series>-<Face>/` (`Arclight-Solstice/`, `Arclight-Pulsar/`, …). Package id `com.<series>.<face>` (`com.arclight.solstice`). Per-face tag drives releases: `arclight-solstice-v0.2.0` → slug `arclight-solstice` → builds+releases the `Arclight-Solstice/` folder (CI unchanged — it splits the slug at `-v`). Git author: simpleapps108@gmail.com.
5. Current state: **Solstice + Pulsar each shipped as their own APK at v0.2.0** (`arclight-solstice-v0.2.0`, `arclight-pulsar-v0.2.0`). Solstice = the byte-identical confirmed-good v0.1.11 face, now standalone (no face-switch). Pulsar = the instrument cluster (dashed battery gauge, live seconds rim, condensed time) with its OWN 4 complications (3 satellites + header). See PROGRESS.md ROUND 10 for the restructure + the full round history + every WFF gotcha.
6. **History note:** the v0.1.x line was "one Arclight APK, 2 faces via a `ListConfiguration` switch" — the face-switch WORKED on-wrist, but complications can't be per-face in one APK (Scene-global), so the owner chose one-APK-per-face and the line was restructured. Solstice is settled; Pulsar's per-face-complication build awaits owner on-wrist confirmation.
7. **SERIES 02 · LEDGER — Phase 1 design DONE (2026-07-11):** `FABLE-Ledger-Showcase.html` specifies 5 typographic faces, all WFF v1, one APK per face: CANON (masthead stack), PARAGON (poster overlap), PICA (complications-as-ledger-rows), BREVIER (word clock — gated static words, no `%` math), NONPAREIL (didone numeral dial + hands). Every signature is mapped to an on-wrist-proven mechanism; possible-but-costly extras carry explicit build-cost notes. LEDGER builds are a later, owner-gated phase — see the LEDGER section in PROGRESS.md.
8. **SERIES 03 · ARMATURE — Phase 1 design DONE (2026-07-11):** `FABLE-Armature-Showcase.html` — 5 mechanical faces, all WFF v1, one APK per face: CALIPER (skeleton + live gear via centre-pivot SecondHand art), SECTOR (retrograde minute fan via [MINUTE] angle Transform), ODOMETER (counter windows, zero risky expressions), ORRERY (orbiting orbs = hand artwork), REGULATOR (3 separated AnalogClock axes). Feasibility-locked like LEDGER; dark-mode line rule stated per face; builds owner-gated.
9. **NEXT (Series 01): flesh out PULSAR** (`Arclight-Pulsar/`, showcase `#f=1`) to the full showcase spec — master gauge as a *configurable complication* (currently battery-drawn), value-driven satellite ring-fill, notif-dots, 5 Reactor/Redline/… themes (currently literal Reactor), Chakra Petch font, regenerate Pulsar preview.png (currently a Solstice placeholder), enrich AOD. Then faces 03-05 (Eclipse/Meridian/Tideline), each its own APK. Validate + adversarial-review each ship.

Also in Resources: **`WFF-Design-Guidelines.md`** — the designer-facing do/don't rulebook (share it with design-only sessions; keep it in sync with runtime-gotchas.md when new on-wrist findings land).

## Standing rule for this project: load BOTH skills in D:\Apps\WearOS Apps\WatchFaces\Resources, every session
- **`FableMode.md`** — the five-gate working method (scope → evidence → attack → verify → report). Apply on every non-trivial task. WFF's runtime is unforgiving and schema-valid ≠ works-on-wrist, so Gate 2 (open the XSD, don't design from memory) and Gate 4 (validate before ship) are load-bearing.
- **`wff-watchface.skill`** — the WFF authoring skill (a zipped bundle: SKILL.md + references/). Its `references/runtime-gotchas.md` holds every "validator passes but the watch shows nothing" fact we learned on-wrist (config-branching limits, which expressions survive, editability/signing traps, animation reality). Read it before writing any dynamic behaviour or configuration. Keep it updated: fold new on-wrist findings back into that file (unzip → edit → re-zip with FORWARD-slash entry names under a `wff-watchface/` root; .NET CreateFromDirectory writes backslashes and corrupts it — build entries via Resolve-Path -Relative).

## Key workflow facts (don't relearn the hard way)
- **Validate before every ship:** WFF XML is NOT checked by the Gradle build. Run google/watchface `wff-validator.jar` (needs Java 17; a portable JRE17 + the jar + `wff-xsd.zip` are in the last session's scratchpad, or re-download from the `latest` release). CI also runs it as a hard gate.
- **Ship loop:** edit → validate → bump versionCode/versionName in Arclight/app/build.gradle → commit → push main (CI validates+builds) → `git tag arclight-vX.Y.Z` + push tag (CI releases the APK). Poll the DIRECT release URL for HTTP 302 (GitHub unauth API rate-limits fast). Paste the direct .apk link to the owner every time.
- **Stable debug key** is committed (`Arclight/arclight-debug.keystore`) so `adb install -r` updates in place — never regenerate it.
- **WFF expression rules proven on-device:** only simple patterns survive — single-boolean gate `[CONFIG.x] ? A : B` and time-range ternary on a PartDraw. Do NOT stack config+time+math on one element (kills it). No string-literal Parameters (numeric `%d` only). Smooth seconds = native `AnalogClock > SecondHand > Sweep frequency="15"`, NOT expression rotation. Fonts have no letterSpacing.
